# 시크릿
.env
.env.local
.env.*.local
.mcp.json
.claude/settings.local.json
.claude/.credentials.json
*.pem
*.key

# 검토용 뷰어 (로컬 전용)
.superpowers/

# 도구 런타임 상태 (로컬 전용)
.bkit/

# Node
node_modules/
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo
data/raw.prev.json
# 답변 채점 캐시 — 키에 프롬프트가 들어 있어 스스로 무효화된다 (재현은 다시 돌리면 된다)
data/answer-cache.json
output/history/answer-cache.json
# 1·2단계 후보 캐시 — 수집을 다시 돌릴 때만 쓰는 중간물
data/history/candidates.json
.vercel
